#include <stdio.h>
#include "main.h"
/**
* 3-islower : a function print alphabet 10x
*
* Return: Always 0 (Succes)
*/
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
return;
}
